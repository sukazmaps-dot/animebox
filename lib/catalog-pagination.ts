/**
 * Catalog pagination is aligned to the desktop 5-column and mobile 2-column
 * grids. 20 fills both layouts cleanly (4x5 desktop, 10x2 mobile) and also
 * lets the ad break split the page into two balanced 10-card blocks.
 */
export const CATALOG_PAGE_SIZE = 20;
export const CATALOG_AD_BREAK_INDEX = 10;
