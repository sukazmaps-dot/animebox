services:
  media-ru:
    build: .
    restart: unless-stopped
    environment:
      PORT: "8788"
      CACHE_DIR: /data/posters
    volumes:
      - animebox-media-cache:/data/posters
    ports:
      - "127.0.0.1:8788:8788"

volumes:
  animebox-media-cache:
